import React from 'react';
import Navbar from '../components/navbar';

const Home: React.FC = () => {
    return (
        <div className="home_wrapper">
            <Navbar />
        </div>
    );
};

export default Home;
